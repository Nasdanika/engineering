Amount of time between two [time points](Temporal.html). 
Can be defined as start and duration or as start and end. 
Duration and end are mutually exclusive. 
When one is specified the other is derived from it.